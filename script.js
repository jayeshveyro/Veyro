const nav=document.querySelector('.nav'),menu=document.querySelector('#menu');menu.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.textContent=open?'×':'☰'});document.querySelectorAll('.nav nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));document.querySelector('#form').addEventListener('submit',e=>{e.preventDefault();document.querySelector('#success').hidden=false;const b=e.target.querySelector('button');b.textContent='Request received ✓';b.disabled=true;e.target.querySelectorAll('input,select').forEach(x=>x.disabled=true)});